# MDCA Demo VM SSH Configuration - RHEL 9.6
# Generated by Terraform for VS Code Remote-SSH extension
# 
# To use this configuration:
# 1. Copy this content to your SSH config file: C:\Users\User\.ssh\config
# 2. Open VS Code
# 3. Install Remote-SSH extension if not already installed
# 4. Press Ctrl+Shift+P and select "Remote-SSH: Connect to Host"
# 5. Select "mdca-demo-rhel96-${suffix}" from the list
#
# Manual SSH connection:
# ssh mdca-demo-rhel96-${suffix}

Host mdca-demo-rhel96-${suffix}
    HostName ${vm_public_ip}
    User ${admin_user}
    IdentityFile C:/Users/User/.ssh/${private_key}
    StrictHostKeyChecking no
    UserKnownHostsFile /dev/null
    ServerAliveInterval 60
    ServerAliveCountMax 3
    
# Alternative host entry for direct IP connection
Host mdca-rhel96-${suffix}
    HostName ${vm_public_ip}
    User ${admin_user}
    IdentityFile C:/Users/User/.ssh/${private_key}
    StrictHostKeyChecking no
    UserKnownHostsFile /dev/null
    ServerAliveInterval 60
    ServerAliveCountMax 3

# ================================================
# MDCA Demo VM Connection Details - RHEL 9.6
# ================================================
# VM Name: mdca-demo-rhel96-${suffix}
# Public IP: ${vm_public_ip}
# Username: ${admin_user}
# SSH Key: C:/Users/User/.ssh/${private_key}
# OS: Red Hat Enterprise Linux 9.6
# 
# Quick Commands:
# - Connect: ssh mdca-demo-rhel96-${suffix}
# - Copy files: scp file.txt mdca-demo-rhel96-${suffix}:/tmp/
# - Port forward: ssh -L 8080:localhost:8080 mdca-demo-rhel96-${suffix}
# 
# VS Code Remote-SSH:
# - Install extension: ms-vscode-remote.remote-ssh
# - Connect via Command Palette: "Remote-SSH: Connect to Host"
# 
# RHEL 9.6 Specific Notes:
# - Package management: dnf (not apt)
# - Firewall: firewalld (not ufw)
# - SELinux is enabled
# - Default shell: bash
# ================================================
